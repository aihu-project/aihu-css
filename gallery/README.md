# Animation gallery data

The generated CSS and class list in this directory are the standalone visual
regression surface for the animation catalog. Regenerate them with
`bun run gen:animations-gallery` after building the Rust core.
